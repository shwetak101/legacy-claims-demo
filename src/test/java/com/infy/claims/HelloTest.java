package com.infy.claims;

import org.junit.Test;

import static org.junit.Assert.assertEquals;

/**
 * TODO: write real tests. Coverage is basically zero. CLM-1099 (open since 2018).
 */
public class HelloTest {

    @Test
    public void sanity() {
        assertEquals(2, 1 + 1);
    }
}
